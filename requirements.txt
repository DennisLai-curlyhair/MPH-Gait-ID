# Bounded core dependencies. Install a CUDA-matched PyTorch build first when
# GPU deployment requires a version different from pip's default wheel.
numpy>=1.24,<2.0
opencv-python>=4.8,<4.11
Pillow>=10.0,<11.0
PyYAML>=6.0,<7.0
torch>=2.6,<3.0
einops>=0.8.2,<0.9
packaging>=23.0,<25.0
